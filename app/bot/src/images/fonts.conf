<?xml version="1.0"?>
<!DOCTYPE fontconfig SYSTEM "urn:fontconfig:fonts.dtd">
<!-- Read by deck-image.ts: only the directory this file sits in, next to the
     bundled Poppins, so fontconfig never scans the system font directories. -->
<fontconfig>
  <dir prefix="relative">.</dir>
</fontconfig>
